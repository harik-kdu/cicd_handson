version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21

  build:
    commands:
      - echo "Building app using Maven..."
      - mvn clean package -DskipTests

artifacts:
  files:
    - target/*.jar